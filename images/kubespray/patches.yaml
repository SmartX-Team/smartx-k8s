---
debian: {{- .Values.build.debian | toYaml | nindent 2 }}

kube: {{- .Values.build.kube | toYaml | nindent 2 }}

kubespray:
  repo:
    revision: {{ printf "v%s" .Values.build.kubespray.version | quote }}
